# Data source to SQL DB
database.user = sa
database.password =
database.connectionString = jdbc:h2:database/~test
hibernateImport = /scripts/test-data.sql

# Hibernate configuration
# Hibernate SQL dialect - H2Dialect embedded DB
hibernate.dialect=org.hibernate.dialect.H2Dialect
# Hibernate DDL settings
hibernate.ddl=create-drop